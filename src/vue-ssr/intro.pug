section
  section
    h3 什么是服务端渲染
    | 在服务端渲染vue组件，将生成的html返回
    pre
      code.
        const App = require('App.vue') // root component
        const app = new Vue({
          render(h) { return h(App) }
        })
        server.get('/', (req, res) => {
          res.type = 'html'
          res.body = renderToHtml(app)
        })
  section
    p 和prerender有何不同？
    ul
      li prerender是将应用跑在浏览器环境得到的html
      li SSR是将单个组件通过框架运行时得出渲染后的html

section
  h3 Vue SSR的流程
  img(src="./ssr.png", alt="Vue SSR的流程图")